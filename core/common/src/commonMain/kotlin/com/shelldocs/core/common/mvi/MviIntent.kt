package com.shelldocs.core.common.mvi

/**
 * Marker for every user/system intention handled by an [MviViewModel].
 * Intents are the only entry point to mutate state in the MVI loop.
 */
interface MviIntent
