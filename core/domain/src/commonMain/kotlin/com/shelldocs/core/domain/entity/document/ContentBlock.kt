package com.shelldocs.core.domain.entity.document

/**
 * One semantic block of a parsed Markdown document.
 * Subtypes live in sibling files (one declaration per file).
 */
sealed interface ContentBlock
